# SullyOS 项目级开发规则

本文件适用于整个 SullyOS 仓库。所有在本仓库工作的 Codex 窗口和子任务都必须遵守。

## 1. 项目目标

SullyOS 是一个 local-first 的虚拟手机与长期角色陪伴系统。本 fork 的目标是在保留原项目能力、数据兼容性和用户共同经历的前提下，逐步增加个人化监督、iOS 快捷指令、MCP 外部数据读取、可靠记忆与长期聊天管理。

最高优先级是：

1. 保护原始聊天记录、Memory Palace、角色资料和用户备份。
2. 保持原 SullyOS 备份可导入，新版本数据可可靠导出和恢复。
3. 默认保持原版行为；新功能只对用户明确启用或配置的范围生效。
4. 让项目可以长期维护、验证、回退和继续交接。

## 2. 开发前必读

开始任何分析、修复或功能开发前，必须完整阅读：

- `docs/HANDOFF.md`

如果任务涉及某个已有专项文档，还必须阅读 `docs/` 中与该功能直接相关的文档。

不得只根据聊天上下文、文件名或旧印象猜测当前状态。应先核对当前分支、Git 状态、相关代码和测试。

如果 `docs/HANDOFF.md` 与实际代码或 Git 历史不一致：

1. 暂停依赖该冲突结论的修改。
2. 通过代码、提交历史和用户已确认信息查明事实。
3. 无法确认时标记为“未知”，不要猜测。
4. 在本次工作结束前修正文档。

## 3. 修改前理解已有架构

修改前必须先定位并理解：

- 当前功能的入口、状态来源、持久化位置和调用链。
- 与 `types.ts`、`context/OSContext.tsx`、`utils/db.ts`、`apps/Chat.tsx`、`hooks/useChatAI.ts`、备份和 Memory Palace 的关系。
- 是否存在旧数据迁移、可选字段、角色卡分享剥离或完整备份往返要求。
- 原版 SullyOS 行为与本 fork 新行为之间的边界。
- 当前是否已有工具函数、组件、资源解析或测试可以复用。

优先复用现有架构和备份资源逻辑，不为相同问题另造互不兼容的第二套系统。

## 4. 功能与设计保护

- 不随意删除、隐藏、重命名或改变已有功能。
- 不因代码看似未使用就直接删除；先确认运行时引用、动态导入、备份兼容和历史迁移用途。
- 不推翻 `docs/HANDOFF.md` 中已经由用户确认的设计决定。
- 如果新需求与已确认决定冲突，先向用户说明冲突、影响和可选方案，得到确认后再修改。
- 不把“重构方便”当作改变产品语义的授权。
- 不扩大任务范围，不顺手修改无关业务。
- Memory Palace、聊天存储、备份导入导出和清理功能属于高风险区域；修改必须有明确理由、最小范围和可验证的失败保护。
- 涉及删除本地消息或用户数据时，必须先有可验证的导出/恢复路径，并再次获得用户明确授权。

## 5. 兼容与隐私

- 新增持久化字段默认应为可选，旧角色和旧备份缺失字段时必须安全降级。
- 完整系统备份应保留必要的新配置；可分享角色卡不得携带 API Key、MCP 本机配置、快捷指令链接或运行时私密状态。
- 不把工具结果伪造成用户消息或静默污染原始聊天历史，除非用户明确要求改变这一设计。
- 不在日志、文档、测试快照或提交信息中写入真实 API Key、Token、私人服务地址或敏感聊天内容。

## 6. 实施与验证

- 修改应尽量小而可审查，每个提交只表达一个清晰目的。
- 修复问题时优先添加或更新能覆盖回归的测试。
- 至少运行与改动直接相关的测试；高风险或跨模块改动应运行完整测试和生产构建。
- 默认验证命令：

```bash
pnpm test:run
pnpm build
```

- 外部 MCP、iOS 快捷指令、PWA、VPN/CORS、真实 API 和文件保存无法仅靠单元测试证明，必须明确记录尚需人工验证的部分。
- 测试失败时不得隐瞒、删除测试或把失败描述为通过。应记录失败命令、现象、可能原因和剩余风险。

## 7. 完成后的交接要求

每次产生实质性修改后，必须同步更新 `docs/HANDOFF.md`，至少记录：

1. 修改了什么。
2. 为什么这样设计。
3. 涉及的重要文件和数据结构。
4. 已完成的自动测试与人工验证。
5. 测试结果。
6. 仍未解决的问题、未知项和后续计划。
7. 是否影响备份兼容、角色卡隐私、Memory Palace 或原始聊天数据。

如果本次只做代码阅读或分析、没有修改功能，也应在结论会影响未来开发路线时更新 HANDOFF；纯临时排查且没有形成新事实时可以不更新。

交付时必须向用户说明：

- 实际修改的文件。
- 当前测试/构建状态。
- 未验证或仍有风险的部分。
- 推荐的下一步。

不得用“已完成”掩盖仍需要用户选择、真实设备验证或外部服务恢复的工作。
